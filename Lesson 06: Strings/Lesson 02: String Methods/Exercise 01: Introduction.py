# Python comes with built-in string methods that gives you
# the power to perform complicated tasks on strings very quickly and efficiently. 

# A string method is called at the end of a string
# and each one has its own method specific arguments.
