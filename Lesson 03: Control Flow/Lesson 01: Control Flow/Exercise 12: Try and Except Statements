def raises_value_error():
  # Added try
  try:
    raise ValueError
  # Added except for ValueErrors
  except ValueError:
    # Print message
    print("You raised a ValueError!")

# Call raises_value_error function
raises_value_error()
