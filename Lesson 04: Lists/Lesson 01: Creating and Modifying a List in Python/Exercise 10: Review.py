# Create list
first_names = ['Ainsley', 'Ben', 'Chani', 'Depak']

# Create empty list
age = []
# Append to age
age.append(42)

# Create and add two list
all_ages = [32, 41, 29] + age

# Create zip using first names and all ages
name_and_age = zip(first_names, all_ages)

# Create range from 0 to 3
ids = range(4)
